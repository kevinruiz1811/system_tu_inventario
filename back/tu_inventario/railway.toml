# Railway: root del servicio = carpeta de este Laravel (back/tu_inventario)
[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'php artisan migrate --force && php artisan optimize:clear && php artisan serve --host=0.0.0.0 --port=$PORT'"
restartPolicyType = "ON_FAILURE"
