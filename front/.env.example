# Desarrollo: deja vacío para usar el proxy de Vite (/api -> http://127.0.0.1:8000).
# Producción (p. ej. Vercel): URL completa del API Laravel, sin barra al final.
# Ejemplo: VITE_API_URL=https://tu-backend.com/api
VITE_API_URL=
